import { GalleryTemplateDirective } from './gallery-template.directive';

describe('GalleryTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new GalleryTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
